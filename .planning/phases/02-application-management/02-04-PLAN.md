---
phase: 02-application-management
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - app/page.tsx
  - app/components/DashboardStats.tsx
  - app/components/AgentShowcase.tsx
autonomous: false
requirements: [APP-02]

must_haves:
  truths:
    - "Dashboard is the landing page showing registered applications overview"
    - "Dashboard shows total number of registered applications"
    - "Dashboard has a quick-view section of recent applications with links to /applications"
    - "Dashboard shows placeholder sections for Last Run and Recent Scripts with empty/coming-soon states"
    - "Dashboard has a Meet Your Agents section with Planner, Executor, Healer agents and subtle animation"
    - "Dashboard feels like a command center with all key info visible at a glance"
  artifacts:
    - path: "app/page.tsx"
      provides: "Dashboard page replacing the Phase 1 placeholder landing page"
      min_lines: 40
    - path: "app/components/DashboardStats.tsx"
      provides: "Stats cards showing application count and other metrics"
      exports: ["DashboardStats"]
    - path: "app/components/AgentShowcase.tsx"
      provides: "Meet Your Agents section with animated agent cards"
      exports: ["AgentShowcase"]
  key_links:
    - from: "app/page.tsx"
      to: "lib/applications.ts"
      via: "listApplications() for app count and recent apps"
      pattern: "import.*listApplications.*from.*applications"
    - from: "app/page.tsx"
      to: "app/components/DashboardStats.tsx"
      via: "component render with stats data"
      pattern: "DashboardStats"
    - from: "app/page.tsx"
      to: "app/components/AgentShowcase.tsx"
      via: "component render"
      pattern: "AgentShowcase"
---

<objective>
Build the TestForge dashboard as the landing page with application overview, placeholder sections, and Meet Your Agents showcase.

Purpose: Transform the landing page into a command center that gives users immediate visibility into their registered applications and establishes the TestForge identity with agent showcasing. Placeholder sections establish the app's future capabilities from Phase 2.

Output: A polished, informative dashboard page at `/` that replaces the Phase 1 placeholder.
</objective>

<execution_context>
@C:/Users/saksh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/saksh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-application-management/02-CONTEXT.md
@.planning/phases/02-application-management/02-01-SUMMARY.md
@.planning/phases/02-application-management/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 02-01 that this plan consumes -->

From lib/applications.ts (created in 02-01):
```typescript
export type ApplicationListItem = {
  id: string;
  name: string;
  testUrl: string;
  createdAt: Date;
  updatedAt: Date;
};

export async function listApplications(): Promise<ApplicationListItem[]>;
```

From app/components/TopNav.tsx (created in 02-02):
```typescript
// TopNav is already integrated in layout.tsx
// Dashboard route (/) will show TopNav with "Dashboard" active state
```
</interfaces>

<skill_reference>
Use the `frontend-design` skill for polished, production-grade UI.
Read: C:/Users/saksh/.claude/skills/frontend-design/SKILL.md

Key design decisions from CONTEXT.md:
- Dashboard is the landing page showing:
  - Applications listed so far -- card or list of registered apps with quick stats
  - Last run -- placeholder section (empty state in Phase 2)
  - Recent scripts -- placeholder section (empty state in Phase 2)
  - "Meet Your Agents" section -- Planner, Executor, Healer with subtle animation
  - Useful metrics -- total apps registered, other stats as available
- Dashboard should feel like a command center
- "Meet Your Agents" section should have subtle animation even as a placeholder
- Light theme with purple/blue accents
- Sections without data show well-designed empty/placeholder states
</skill_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard page with stats, app overview, and placeholder sections</name>
  <files>app/page.tsx, app/components/DashboardStats.tsx</files>
  <action>
**Replace `app/page.tsx` with the Dashboard:**

This is a Server Component. It fetches data server-side.

1. Import `listApplications` from `@/lib/applications`
2. Call `await listApplications()` to get applications
3. Compute metrics: total apps count, most recently updated app

Layout the dashboard with these sections (per user decisions):

**Section 1: Welcome / Header Area**
- "Welcome to TestForge" or similar greeting
- Brief tagline about AI-powered test generation
- Quick action button: "Add Application" linking to `/applications` (or opening the create modal)

**Section 2: Stats Cards (DashboardStats component)**
- Total Applications registered (actual count)
- Tests Generated (placeholder: "0" or "--")
- Active Agents (placeholder: "3" or show Planner, Executor, Healer)
- Success Rate (placeholder: "--%" or "N/A")
- Use the purple/blue theme for card accents

**Section 3: Applications Overview**
- Show up to 5 most recent applications in a compact list/card format
- Each shows: name, URL, how long ago created
- "View All" link to `/applications`
- If no applications: "No applications registered yet. Add your first application to get started." with a link to `/applications`

**Section 4: Last Run (Placeholder)**
- Title: "Last Run" or "Recent Activity"
- Well-designed empty state: "No test runs yet. Your recent test executions will appear here."
- Subtle icon or illustration
- Per user decision: placeholder section showing empty state in Phase 2

**Section 5: Recent Scripts (Placeholder)**
- Title: "Recent Scripts"
- Well-designed empty state: "No scripts generated yet. Your generated Playwright scripts will appear here."
- Per user decision: placeholder section showing empty state in Phase 2

**Create `app/components/DashboardStats.tsx`:**

Client or Server component showing metric cards in a row/grid.

Props: `appCount: number` (other stats are placeholder)

Display 3-4 metric cards in a horizontal row:
- Each card: icon, label, value
- Use the purple/blue accent colors
- Subtle shadow and rounded corners
- Per user decision: useful metrics -- total apps registered, other stats as available

Use frontend-design skill: make the dashboard feel like a command center. Avoid generic card layouts -- add character through typography, color, spacing, and subtle design touches that make it feel like a professional SaaS tool.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard page at / shows welcome section, stats cards with app count, applications overview (empty or populated), Last Run placeholder, Recent Scripts placeholder. All sections have polished styling with purple/blue theme.</done>
</task>

<task type="auto">
  <name>Task 2: Meet Your Agents showcase with subtle animation</name>
  <files>app/components/AgentShowcase.tsx</files>
  <action>
**Create `app/components/AgentShowcase.tsx`:**

Client component (`"use client"`) for the "Meet Your Agents" section.

Per user decisions:
- "Meet Your Agents" section -- list of agents with subtle animation
- Should have subtle animation even as a placeholder -- makes the app feel alive
- Three agents: Planner, Executor, Healer

Design 3 agent cards:

1. **Planner Agent**
   - Icon: brain/planning icon (use emoji or SVG)
   - Description: "Analyzes your test scenarios and creates structured, professional testing plans"
   - Status: "Ready" (placeholder)

2. **Executor Agent**
   - Icon: play/run icon
   - Description: "Executes test plans in a real browser using Playwright's testing framework"
   - Status: "Ready" (placeholder)

3. **Healer Agent**
   - Icon: wrench/fix icon
   - Description: "Automatically fixes broken test scripts when application UI changes"
   - Status: "Ready" (placeholder)

**Subtle animations (per user decision):**
- Use CSS animations (not a JS animation library -- keep it lightweight)
- Each agent card could have a subtle glowing pulse effect on its icon/status indicator
- Staggered entrance animation when the section scrolls into view or on page load
- A gentle breathing/pulsing "status light" for each agent (small colored dot that slowly pulses)
- Keep animations smooth and non-distracting -- they should make the app feel alive, not busy

Style consistently with the dashboard. Use the purple/blue theme. Cards should feel premium and intentional.

**Integrate into `app/page.tsx`:**
Add the AgentShowcase component to the dashboard page, positioned after the main content sections.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>AgentShowcase renders 3 agent cards (Planner, Executor, Healer) with descriptions, status indicators, and subtle CSS animations (pulsing status lights, staggered entrance). Integrated into dashboard page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dashboard layout, content, and animations</name>
  <action>
What was built: Complete TestForge dashboard at / with stats cards, application overview, placeholder sections (Last Run, Recent Scripts), and animated Meet Your Agents showcase. Top navigation highlights Dashboard when on this page.

How to verify:
1. Start dev server: `npm run dev`
2. Navigate to `http://localhost:3000` (or assigned port)
3. Verify Dashboard is the landing page with:
   - Stats cards showing application count
   - Applications overview section (empty state or populated list)
   - Last Run placeholder with empty state design
   - Recent Scripts placeholder with empty state design
   - Meet Your Agents section with 3 agent cards (Planner, Executor, Healer)
4. Verify Meet Your Agents has subtle animations (pulsing status lights, entrance animation)
5. Verify the overall feel is "command center" -- professional, informative, alive
6. Verify purple/blue theme is consistent across all dashboard sections
7. Verify TopNav shows Dashboard as active
8. Click "Applications" in the nav to verify navigation to /applications works
9. If apps were created in Plan 02-03 verification, verify the dashboard shows them in the overview section
  </action>
  <verify>Human verification of dashboard</verify>
  <done>User has confirmed the dashboard looks and feels like a command center with all required sections, correct data, and subtle animations. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors
2. `npx next build` -- build succeeds
3. `npx vitest run` -- all tests pass
4. Dashboard at / shows all required sections with polished design
5. Meet Your Agents has visible subtle animations
</verification>

<success_criteria>
- Dashboard replaces Phase 1 placeholder landing page
- Stats cards show actual application count
- Applications overview shows recent apps or empty state
- Last Run and Recent Scripts show well-designed placeholder states
- Meet Your Agents section shows 3 agents with subtle animation
- Purple/blue theme consistent with rest of the application
- Dashboard feels like a command center (per user decision)
- Build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-application-management/02-04-SUMMARY.md`
</output>
