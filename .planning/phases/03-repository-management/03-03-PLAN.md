---
phase: 03-repository-management
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - app/applications/[id]/components/ConnectRepoModal.tsx
  - app/applications/[id]/components/RepositoryList.tsx
  - app/applications/[id]/components/RepositoryRow.tsx
  - app/applications/[id]/components/EditRepoRow.tsx
  - app/applications/[id]/components/DeleteRepoDialog.tsx
  - app/applications/[id]/components/AppDetailClient.tsx
autonomous: false
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04
  - REPO-05

must_haves:
  truths:
    - "User can open Connect Repository modal from the detail page"
    - "Connect modal has a GitHub/ADO toggle that adjusts form fields"
    - "GitHub form shows repo URL + PAT fields; ADO form adds organization name field"
    - "Output folder field is pre-filled with tests/{slugified-app-name}"
    - "PAT field uses PasswordField component with show/hide toggle"
    - "Submitting the form validates PAT via API call and shows specific error on failure"
    - "Successfully connected repo appears in the repository list"
    - "Repository list shows compact rows with provider icon, repo name, output folder, and action buttons"
    - "User can edit the output folder inline from the repo row"
    - "User can delete a repo via confirmation dialog"
    - "Empty state shows dashed border with No repositories connected message"
  artifacts:
    - path: "app/applications/[id]/components/ConnectRepoModal.tsx"
      provides: "Connect repository form modal with provider toggle"
      contains: "ConnectRepoModal"
    - path: "app/applications/[id]/components/RepositoryList.tsx"
      provides: "Repository list with empty state"
      contains: "RepositoryList"
    - path: "app/applications/[id]/components/RepositoryRow.tsx"
      provides: "Single repo row with provider icon, name, folder, actions"
      contains: "RepositoryRow"
    - path: "app/applications/[id]/components/EditRepoRow.tsx"
      provides: "Inline edit mode for output folder on a repo row"
      contains: "EditRepoRow"
    - path: "app/applications/[id]/components/DeleteRepoDialog.tsx"
      provides: "Delete confirmation dialog for repository removal"
      contains: "DeleteRepoDialog"
  key_links:
    - from: "app/applications/[id]/components/ConnectRepoModal.tsx"
      to: "lib/actions/repositories.ts"
      via: "connectRepositoryAction server action"
      pattern: "connectRepositoryAction"
    - from: "app/applications/[id]/components/DeleteRepoDialog.tsx"
      to: "lib/actions/repositories.ts"
      via: "deleteRepositoryAction server action"
      pattern: "deleteRepositoryAction"
    - from: "app/applications/[id]/components/AppDetailClient.tsx"
      to: "app/applications/[id]/components/RepositoryList.tsx"
      via: "renders repo list with data from server component"
      pattern: "RepositoryList"
---

<objective>
Build the repository connection UI: the Connect Repository modal with GitHub/ADO toggle, the repository list with compact rows, inline output folder editing, and repository deletion dialog. Wire everything into the application detail page.

Purpose: Complete the repository management user experience -- users can connect repos with validated PATs, see their connected repos, edit output folders, and remove repos.
Output: Fully functional repository management UI integrated into the application detail page.
</objective>

<execution_context>
@C:/Users/saksh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/saksh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-repository-management/03-CONTEXT.md
@.planning/phases/03-repository-management/03-01-SUMMARY.md
@.planning/phases/03-repository-management/03-02-SUMMARY.md

<!-- Source files -->
@app/applications/[id]/components/AppDetailClient.tsx
@app/applications/components/ApplicationModal.tsx
@app/applications/components/PasswordField.tsx
@app/applications/components/DeleteDialog.tsx
@lib/repositories.ts
@lib/actions/repositories.ts
@lib/schemas/repository.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/repositories.ts (created in Plan 01):
```typescript
export type RepositoryListItem = { id: string; provider: string; repoUrl: string; outputFolder: string; organization: string | null; createdAt: Date; updatedAt: Date; };
export function extractRepoName(urlString: string, provider: "github" | "ado"): string;
export function slugify(text: string): string;
```

From lib/actions/repositories.ts (created in Plan 01):
```typescript
export async function connectRepositoryAction(applicationId: string, prevState: ActionState, formData: FormData): Promise<ActionState>;
export async function updateRepositoryAction(repoId: string, prevState: ActionState, formData: FormData): Promise<ActionState>;
export async function deleteRepositoryAction(repoId: string, applicationId: string): Promise<ActionState>;
```

From lib/actions/applications.ts:
```typescript
export type ActionState = { success: boolean; errors?: Record<string, string[]>; message?: string; };
```

From lib/applications.ts:
```typescript
export type ApplicationView = { id: string; name: string; testUrl: string; testUsername: string; testPassword: string; createdAt: Date; updatedAt: Date; };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ConnectRepoModal with provider toggle and form</name>
  <files>app/applications/[id]/components/ConnectRepoModal.tsx</files>
  <action>
    Create ConnectRepoModal following the existing ApplicationModal pattern (backdrop, close button, escape key, backdrop click):

    **Props:**
    ```typescript
    interface ConnectRepoModalProps {
      applicationId: string;
      applicationName: string;  // For generating default output folder
      isOpen: boolean;
      onClose: () => void;
    }
    ```

    **Provider Toggle** (Claude's Discretion -- segmented control):
    - Two-button segmented control at the top of the form: "GitHub" and "Azure DevOps"
    - Active state: bg-surface text-text-primary shadow-sm
    - Inactive state: text-text-muted hover:text-text-secondary
    - Container: flex rounded-lg border border-border bg-surface-dim p-0.5
    - Use local state: `const [provider, setProvider] = useState<"github" | "ado">("github")`
    - Hidden input: `<input type="hidden" name="provider" value={provider} />`

    **Form fields:**
    - **Repository URL** (always shown): text input, placeholder differs by provider
      - GitHub: "https://github.com/org/repo"
      - ADO: "https://dev.azure.com/org/project/_git/repo"
    - **Organization** (ADO only, conditionally rendered): text input, placeholder "Organization name"
    - **Personal Access Token**: reuse `<PasswordField>` component from `@/app/applications/components/PasswordField`
      - name="pat", label="Personal Access Token"
    - **Output Folder**: text input, defaultValue=`tests/${slugify(applicationName)}`
      - Import `slugify` from `@/lib/repositories`
      - Placeholder: "tests/my-web-app"

    **Form submission:**
    - Use `useActionState` with `connectRepositoryAction.bind(null, applicationId)`
    - On success (state.success), call `onClose()`
    - Show field-level errors from state.errors (same pattern as ApplicationModal)
    - Show general error from state.message (for PAT validation failures like "PAT needs 'repo' scope")
    - Submit button: "Connect Repository" / "Connecting..." with spinner

    **Important:** The general error message (state.message) is where PAT validation errors surface. Style it prominently -- red border, red background, clear text. This is the primary feedback mechanism for PAT permission issues.

    **Styling:** Match ApplicationModal dimensions and patterns (max-w-lg, rounded-xl, border, shadow-xl, same button styles).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ConnectRepoModal renders with GitHub/ADO toggle, adjusts fields per provider, submits via connectRepositoryAction, shows PAT validation errors, and closes on success.</done>
</task>

<task type="auto">
  <name>Task 2: Build RepositoryList, RepositoryRow, EditRepoRow, DeleteRepoDialog, and wire into AppDetailClient</name>
  <files>app/applications/[id]/components/RepositoryList.tsx, app/applications/[id]/components/RepositoryRow.tsx, app/applications/[id]/components/EditRepoRow.tsx, app/applications/[id]/components/DeleteRepoDialog.tsx, app/applications/[id]/components/AppDetailClient.tsx</files>
  <action>
    **RepositoryList.tsx:**
    - Props: `repositories: RepositoryListItem[]`, `applicationId: string`, `applicationName: string`, `onConnectClick: () => void`
    - Section wrapper: rounded-xl border border-border bg-surface card
    - Header row: "Connected Repositories" heading + count badge + "Connect Repository" button (calls onConnectClick)
    - If repositories.length === 0: Empty state with dashed border area, "No repositories connected" text, "Connect Repository" button (matches existing empty state pattern from ApplicationsClient)
    - If repositories.length > 0: Render list of RepositoryRow components
    - Each row separated by a border-t divider

    **RepositoryRow.tsx:**
    - Props: `repository: RepositoryListItem`, `applicationId: string`, `onDelete: (id: string, name: string) => void`, `onEdit: (id: string) => void`, `isEditing: boolean`
    - If isEditing=true, delegate to EditRepoRow (or render inline edit form)
    - Normal display (compact row):
      - Left side: Provider icon (GitHub octocat SVG or ADO icon SVG), repo name (extracted via `extractRepoName(repo.repoUrl, repo.provider as "github" | "ado")`), output folder path in muted text
      - Right side (visible on hover, opacity-0 group-hover:opacity-100): Edit (pencil) and Delete (trash) icon buttons
    - Provider icons: Simple SVG icons for GitHub (octocat or mark) and Azure DevOps (diamond shape)
    - Row styling: px-4 py-3 flex items-center justify-between group hover:bg-surface-dim transition-colors

    **EditRepoRow.tsx:**
    - Props: `repository: RepositoryListItem`, `applicationId: string`, `onCancel: () => void`
    - Inline editing for the output folder field only (per user decision, output folder is editable after connection)
    - Single text input for outputFolder pre-filled with current value
    - Save/Cancel buttons
    - Use `useActionState` with `updateRepositoryAction.bind(null, repository.id)`
    - On success, call onCancel to exit edit mode (page revalidates via server action)

    **DeleteRepoDialog.tsx:**
    - Follow the existing DeleteDialog pattern (same structure, backdrop, escape key, loading state)
    - Props: `repositoryId: string`, `repositoryName: string`, `applicationId: string`, `isOpen: boolean`, `onClose: () => void`
    - Message: "Are you sure you want to disconnect {repositoryName}? This will not affect the repository itself."
    - Call `deleteRepositoryAction(repositoryId, applicationId)` on confirm
    - On success, call onClose

    **AppDetailClient.tsx** -- Update to wire repository components:
    1. Add state: `connectRepoOpen: boolean` (for connect modal)
    2. Add state: `deleteRepoTarget: { id: string; name: string } | null` (for delete dialog)
    3. Add state: `editingRepoId: string | null` (for inline output folder editing)
    4. Replace the placeholder "Connected Repositories" section from Plan 02 with:
       - `<RepositoryList>` component passing repositories, applicationId, applicationName, and onConnectClick handler
    5. Render `<ConnectRepoModal>` when connectRepoOpen is true
    6. Render `<DeleteRepoDialog>` when deleteRepoTarget is set
    7. Pass editing state down to RepositoryList -> RepositoryRow for inline edit mode

    **Import paths:**
    - ConnectRepoModal: `./ConnectRepoModal`
    - RepositoryList: `./RepositoryList`
    - DeleteRepoDialog: `./DeleteRepoDialog`
    - PasswordField: `@/app/applications/components/PasswordField` (reused from existing)
    - extractRepoName, slugify: `@/lib/repositories`
    - connectRepositoryAction, updateRepositoryAction, deleteRepositoryAction: `@/lib/actions/repositories`
    - ActionState: `@/lib/actions/applications`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - Repository list displays connected repos with provider icon, name, output folder
    - Empty state shows when no repos connected with "Connect Repository" button
    - Connect Repository modal opens, submits, shows validation errors, closes on success
    - Output folder editable inline from repo row
    - Delete repo shows confirmation dialog, removes repo on confirm
    - All components wired into AppDetailClient
    - Build succeeds with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete repository management</name>
  <files>N/A -- verification only</files>
  <action>
    Human verifies the complete repository management UI built across Plans 02 and 03:
    1. ApplicationCard links to /applications/[id] (no hover edit/delete icons)
    2. Application detail page with inline editing for app fields
    3. Connect Repository modal with GitHub/ADO toggle
    4. Repository list with compact rows (provider icon, name, output folder)
    5. Inline output folder editing on repo rows
    6. Delete repository confirmation dialog
    7. PAT validation with specific error messages

    Steps:
    1. Run `npm run dev` and open http://localhost:3000/applications
    2. Verify ApplicationCards are clickable links (no hover edit/delete icons)
    3. Click a card to navigate to /applications/[id] detail page
    4. Verify app details display: name, URL, username, password (with reveal toggle)
    5. Click "Edit" -- verify all fields become editable, change the name, click "Save Changes"
    6. Verify the name updates and view mode restores
    7. In the "Connected Repositories" section, verify empty state shows
    8. Click "Connect Repository" -- verify modal opens with GitHub selected
    9. Toggle to "Azure DevOps" -- verify organization field appears
    10. Toggle back to "GitHub" -- verify organization field disappears
    11. Fill in a real GitHub repo URL and PAT (or use invalid values to verify error messages)
    12. After connecting, verify the repo appears in the list with correct icon and name
    13. Hover over a repo row -- verify edit (pencil) and delete (trash) buttons appear
    14. Click edit -- verify output folder becomes editable, change it, save
    15. Click delete -- verify confirmation dialog, confirm, repo removed from list
    16. Navigate back to /applications -- verify list still works correctly
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User confirms all repository management flows work correctly: connect, view, edit output folder, delete. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
```bash
# TypeScript compiles
npx tsc --noEmit

# Build succeeds
npm run build

# All tests still pass (no regressions)
npx vitest run

# Dev server starts without errors
npm run dev
```
</verification>

<success_criteria>
1. Connect Repository modal opens with GitHub/ADO toggle that adjusts visible fields
2. GitHub form: repo URL + PAT fields; ADO form: repo URL + PAT + organization fields
3. Output folder pre-filled with tests/{slugified-app-name}
4. PAT validation errors display prominently (e.g., "PAT needs 'repo' scope")
5. Connected repos appear as compact rows with provider icon, extracted name, output folder
6. Output folder editable inline from repo row
7. Delete confirmation dialog removes repo from list
8. Empty state renders when no repos connected
9. All existing functionality (create app, view apps, dashboard) unbroken
10. TypeScript compiles and build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-repository-management/03-03-SUMMARY.md`
</output>
